0.git init (第一次执行即可)
1.git add -A
2.git commit -m "v1, base version,support Button"
3.git tag v1
4.git remote add origin https://github.com/hujinfan/APP_Addons_0001_Message （第一次执行即可）
5.git push -u origin master
6.git push origin --tags

创建一个线程V2
1.git add -A
2.git commit -m "v2, create thread(1)"
3.git tag v2
4.git push -u origin master
5.git push origin --tags

Ctrl+O 复写类成员（函数）

创建第二个线程，此时程序有主线程，线程1，线程2
1.git add -A
2.git commit -m "v3, create thread(2)"
3.git tag v3
4.git push -u origin master
5.git push origin --tags

版本4：支持消息处理
1.git add -A
2.git commit -m "v4, support Message"
3.git tag v4
4.git push -u origin master
5.git push origin --tags
